server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DongmyeongLee22/config-repository
          search-paths: training/licensingservice


management:
  endpoints:
    web:
      exposure:
        include: refresh
